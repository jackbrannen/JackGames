node_modules/
.next/
.env.local
.env
*.env
.DS_Store
supabase/.temp/
**/supabase/.temp/
*.env.local.tmp
*.env.prod.tmp
.env.local.tmp
.env.prod.tmp

.env.temp
**/.env.temp

# Raw source images — not read by any app code, only the generated public/voices/*.webp
# output is actually served. Kept on disk (Dropbox already backs it up) but excluded from
# git so it doesn't bloat repo size/clone time.
HearingVoices/Voices/
